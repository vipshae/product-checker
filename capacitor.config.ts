import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.vercel.productchecker',
  appName: 'product-checker',
  webDir: 'build-static'
};

export default config;
